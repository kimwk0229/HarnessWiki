# HarnessWiki 스키마 및 운영 가이드

> **호환성**: 이 문서는 Claude Code, Codex, GitHub Copilot, 그 외 모든 마크다운 기반 프로젝트 관리 및 AI 협업 도구에서 사용 가능합니다.
>
> **대상**: AI 에이전트(Codex, Claude Code, GitHub Copilot 등) 및 자동화 도구를 통해 HarnessWiki를 운영하는 사용자

## 이 프로젝트란

HarnessWiki는 회의록을 위한 SSOT(Single Source of Truth) 위키다.
Andrej Karpathy의 "LLM Wiki" 패턴을 따른다:
https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f

매번 RAG로 재검색하지 않고, 회의에서 나온 지식을 주제별 마크다운 페이지에
지속적으로 누적·정리한다. "지식은 매번 재발견되지 않고 누적된다."

## 3계층 구조

1. **raw/** — 원본 자료(raw sources). 수동 메모든 STT 전사든, `raw.md`는 저장 후
   **절대 수정하지 않는다**. 오직 새 정보는 새 회의 폴더로만 추가.
   
2. **wiki/** — 주제 페이지들(the wiki). 이 저장소의 핵심 산출물.
   여러 회의의 정보가 누적/합성되는 곳. 상호링크된 마크다운 페이지들이 진화한다.
   
3. **이 파일, index.md, log.md** — 스키마 및 카탈로그 계층(the schema).
   협업자(인간 또는 AI)가 따를 규칙(수집/쿼리/린트)과 전체 내용 목록을 정의한다.

## 디렉토리 규칙

### 원본 보관소 (raw/)
```
raw/
├── YYYY-MM-DD_슬러그/
│   ├── raw.md      # 원문 그대로, 절대 수정 금지
│   └── meta.md     # 제목/일시/참석자/자료유형/한줄요약 + raw.md 링크
```

- 슬러그: 소문자/한글 kebab 형태(예: `기능a-리뷰`, `리스크-검토`).
  같은 날 중복 시 접미사로 구분(예: `2026-08-10_킥오프-회의-오전`, `킥오프-회의-오후`).
- `raw.md`: 회의 원문을 **그대로만** 저장. 수동 메모도, STT 전사도, 그 복합도 모두 가능.
  write-once 정책 — 저장 후 절대 수정 금지.
- `meta.md`: 회의의 메타카드. 제목/일시/참석자 리스트/자료유형/한줄 요약 + `raw.md` 링크 포함.
  주제 페이지에서 회의를 참조할 때는 거대한 `raw.md` 대신 이 가벼운 카드를 가리킴.

### 주제 페이지 (wiki/topics/)
- 형식: `wiki/topics/슬러그/슬러그.md` (폴더 노트 구조)
- 여러 회의에서 나온 같은 주제의 정보를 누적.
- 최근 결정사항, 논의 히스토리(회의별 섹션), 열린 질문, 관련 페이지 등을 담음.
- 추천 구조: `_templates/topic-page.template.md` 참조.

### 롤업 페이지 (자동 합성 뷰)
- `wiki/결정/결정.md`: 모든 주제 페이지에서 결정사항을 롤업.
- `wiki/액션아이템/액션아이템.md`: 모든 회의에서 액션아이템을 체크박스 리스트로 롤업.
- 이 둘은 ingest 절차의 부산물로 계속 갱신됨. **반드시 매 ingest마다 동시 갱신 필수**.

### 참고 페이지 (보조 문서)
- `wiki/보류/보류.md`: 현재 의사결정 보류 중이거나 나중으로 연기된 항목들 추적.
- `wiki/사람/사람.md`: 팀 멤버 및 역할, 책임, 담당 영역 기록.

### 링크 규칙 (경로 깊이)
주제 페이지 위치: `wiki/topics/슬러그/슬러그.md` 기준
- **raw 참조**: `../../../raw/YYYY-MM-DD_슬러그/meta.md` (3단계 상향)
- **형제 토픽 링크**: `../다른-슬러그/다른-슬러그.md` (같은 topics/ 내)
- **결정 롤업 링크** (결정.md에서): `../topics/슬러그/슬러그.md`
- **액션아이템 링크** (액션아이템.md에서): `../../raw/YYYY-MM-DD_슬러그/meta.md`

**중요**: 링크 깊이를 섞어서 쓰지 말 것. raw는 항상 3단계, 형제 토픽은 항상 상대경로로 통일.

### 카탈로그/스키마
- `index.md`: 위키 전체 페이지 카탈로그. 신규 주제 페이지 생성 시 반드시 여기에 등록.
- `log.md`: 시간순 변경 이력. `[INGEST]`, `[QUERY]`, `[LINT]` 태그로 구분.
- **이 파일** (프로젝트 루트의 `.md` 스키마 파일): 규칙 정의.
- `wiki/보류/보류.md`: 보류 항목 추적.
- `wiki/사람/사람.md`: 팀 멤버 및 역할 기록.

### 제외 대상
- `_templates/`: 콘텐츠가 아니므로 린트(고아 페이지 검사)에서 제외.

## 오퍼레이션 1: 수집 (Ingest)

사용자가 새 회의 메모/전사본을 주면 다음 순서로 처리한다:

1. **메타정보 확인**
   - 날짜, 제목, 참석자(가능한 만큼), 자료유형(수동 메모 vs STT)을 확인.
   - 불명확하면 사용자에게 질문.

2. **원본 저장**
   - `raw/YYYY-MM-DD_슬러그/raw.md`에 원문을 **손대지 않고 그대로** 저장.
   - 같은 폴더에 `meta.md` 작성 (`_templates/meeting-meta.template.md` 참조).
   - **rule**: raw.md는 저장 후 절대 수정 금지 (write-once 정책). 오타도 건드리지 말 것.

3. **주제 페이지 검색 및 연계**
   - 원문을 읽고 관련된 기존 주제 페이지를 `wiki/topics/` 전체에서 검색(grep).
   - 없으면 새 주제 페이지를 만들고 `index.md`에 링크 추가.
   - **rule**: 새 토픽은 반드시 `wiki/topics/슬러그/슬러그.md` 폴더 노트 형식으로만 생성.

4. **주제 페이지에 정보 누적**
   - 각 관련 주제 페이지마다 이번 회의의 정보를 추가.
   - **모순이 생기면**: 지우지 말고 "이전에는 X였으나 2026-08-10 현재 Y로 변경 ([회의 링크])" 형태로 변경 이력을 남김.
     이렇게 하면 나중에 왜 바뀌었는지 추적 가능.
   - **참석자/결정/액션아이템**: 강제 필드가 아니라 자연스러운 문장/불릿으로 본문에 녹임.
   - 해당 회의의 `meta.md`로 돌아가는 링크를 **반드시** 포함 (근거 투명성).
   - 주제 페이지 최상단에 마지막 갱신 날짜를 남기기 (선택적이지만 권장: "마지막 갱신: 2026-08-10").

5. **결정사항 롤업**
   - 새 결정사항이 있으면 주제 페이지에 먼저 작성한 후, 
     `wiki/결정/결정.md`에도 동시에 반영.
   - **rule**: ingest 후 결정.md 갱신 누락 시 SSOT 깨짐. 필수.

6. **액션아이템 롤업**
   - 새 액션아이템이 있으면 `wiki/액션아이템/액션아이템.md`에 체크박스 형태로 추가.
   - 형식: `- [ ] 내용 (담당자, 기한 if 있으면 자유 형식) — 출처: [회의명](../../raw/YYYY-MM-DD_슬러그/meta.md)`
   - 강제 필드 아님. 담당자/기한이 없으면 그냥 "- [ ] 내용"도 가능.
   - **rule**: ingest 후 액션아이템.md 갱신 누락 시 SSOT 깨짐. 필수.

7. **링크 유효성 검증**
   - 신규 추가한 모든 링크가 실제 파일을 가리키는지 확인.
   - 특히 raw 메타 링크와 형제 토픽 링크는 필수 검증.
   - **rule**: 깨진 링크로 ingest 완료 금지.

8. **로그 업데이트**
   - `log.md`에 `[INGEST]` 항목 추가.
   - 형식: `- [INGEST] 회의명 → 생성/갱신된 페이지 목록`

9. **사용자에게 보고**
   - 처리 완료 후 생성/갱신된 페이지 목록과 간략한 요약을 제시.

### Ingest 체크리스트

ingest 완료 전 다음을 반드시 확인:
- [ ] raw.md 저장 완료 (수정 금지, write-once 정책 준수)
- [ ] meta.md 작성 완료 (제목/날짜/자료유형/한 줄 요약 포함)
- [ ] 주제 페이지 신규 생성 시 폴더 노트 구조 준수 (`wiki/topics/슬러그/슬러그.md`)
- [ ] 모든 링크 경로 검증 (raw: `../../../raw/`, 형제 토픽: `../슬러그/슬러그.md`)
- [ ] 신규 토픽이면 `index.md`에 등록
- [ ] `wiki/결정/결정.md` 동시 갱신
- [ ] `wiki/액션아이템/액션아이템.md` 동시 갱신
- [ ] `log.md`에 `[INGEST]` 항목 추가
- [ ] 생성/갱신된 파일 목록을 사용자에게 보고

## 오퍼레이션 2: 쿼리 (Query)

사용자 질문에 답할 때:

1. **페이지 검색**
   - `index.md`로 관련 주제 페이지를 찾음.
   - 필요하면 `wiki/` 전체를 grep으로 검색.

2. **위키 기반 답변**
   - 위키에 있는 정보로 답변.
   - 없으면 명시적으로 "위키에는 없음"을 말하고, 
     `raw/`의 원본을 직접 검색해볼지 제안.

3. **재사용 가치 판단**
   - 답변이 단순 조회가 아니라 여러 페이지를 종합한 새로운 통찰이면,
     기존 주제 페이지에 추가하거나 신규 페이지로 저장할지 사용자에게 확인.
   - 저장 시 `log.md`에 `[QUERY]` 항목 추가.

4. **투명성**
   - 답변에는 근거가 된 회의/페이지 링크를 함께 제시.

## 오퍼레이션 3: 린트 (Lint)

"린트해줘" 요청 시 다음을 점검하고 목록으로 보고:

1. **고아 페이지**: `wiki/topics/` 내 각 파일이 `index.md` 또는 다른 페이지에서
   링크되는지 확인. 링크 없는 페이지 리스트업. (`_templates/` 제외)

2. **끊어진 링크**: 상대경로 링크가 실제 존재하는 파일을 가리키는지 확인.
   404인 링크를 모두 보고.

3. **모순**: 같은 주제에 대해 시점 구분 없이 충돌하는 서술이 있는지 확인.
   "A는 X다"와 "A는 Y다"가 동시에 있으면 플래그.

4. **역링크 누락**: A가 B를 링크하는데 B에 A로 돌아가는 역링크가 없는 경우.
   (모든 링크가 양방향일 필요는 없지만, 중요한 역참조는 플래그)

5. **오래된 액션아이템**: `wiki/액션아이템/액션아이템.md`에서 4주 이상 갱신 안 된 항목 플래그.
   마지막 진행 상황 갱신이 필요한 아이템 목록.

6. **index.md 누락**: `wiki/topics/` 내 파일 중 `index.md`에 없는 것 탐지.

발견 사항을 보고한 후, 사용자 승인을 받아 필요한 수정을 하고,
`log.md`에 `[LINT]` 항목 추가.

## 오퍼레이션 4: 검증 (Validation)

ingest 완료 직후, **반드시 다음 순서대로** 실행하는 통합 검증 워크플로우:

```
회의 메모 제공
    ↓
/ingest (회의 수집)
    ↓
/validate-wiki YYYY-MM-DD (통합 검증) ← 필수 규칙
    ↓
모든 검증 통과 또는 발견사항 수정
    ↓
git commit
```

### 검증 구성 (3단계)

#### 1단계: 기계 검증 (Lint)
TypeScript 스크립트(`scripts/lint.ts`)로 자동 검증:
- ✅ Frontmatter YAML 유효성
- ✅ 파일 구조 규칙 (경로, 폴더명, 파일명)
- ✅ 슬러그 규칙 (kebab-case, 한글 허용)
- ✅ 링크 유효성 (깨진 링크, 경로 깊이)

**결과**: `[ERROR]` (0 허용 안 함), `[WARN]` (검토 필요), `[INFO]` (정상 작동)

#### 2단계: 컨텐츠 검증 (LLM)
Claude가 회의 원본과 위키 반영 내용을 비교:
1. **일치도**: 회의 내용과 위키 반영 일치도
2. **할루시네이션**: 회의에 없는 정보 추가 여부
3. **누락**: 중요 내용 누락 여부
4. **액션 추적**: 액션아이템 상태 변화 추적
5. **메타정보/링크**: frontmatter, 링크, 롤업 검증

**결과**: 5가지 항목 각각 PASS / REVIEW / FAIL 판정

#### 통합 리포트
`/validate-wiki YYYY-MM-DD`는 1, 2단계를 순차 실행하고 통합 리포트 생성:
```markdown
# HarnessWiki 검증 리포트

## 1️⃣ 기계 검증 (Lint) 결과
**상태**: ✅ PASS / ⚠️ WARNING / ❌ FAIL

## 2️⃣ 컨텐츠 검증 (LLM) 결과
**상태**: ✅ APPROVED / ⚠️ REVIEW NEEDED / ❌ FAILED

## 📊 최종 판정
✅ 모든 검증 통과 / ⚠️ 검토 필요 / ❌ 검증 실패
```

### 판정 규칙

**✅ APPROVED** (모든 검증 통과)
→ 바로 `git commit` 진행

**⚠️ REVIEW NEEDED** (검토 필요)
→ 발견사항 검토 및 수정 후 재검증
1. 지적 파일 수정
2. 커밋 메시지: `[REVIEW FIX] YYYY-MM-DD 회의 — 검증 지적사항 수정`
3. `/validate-wiki YYYY-MM-DD` 재실행

**❌ FAILED** (심각한 오류)
→ 원본 재검토 및 재ingest 필수
1. 원본 회의록(raw.md) 재검토
2. 잘못된 정보 제거 또는 수정
3. 커밋 메시지: `[REVIEW FIX] YYYY-MM-DD 회의 — 심각한 오류 수정`

### 필수 규칙

- **검증 생략 금지**: ingest 후 검증 없이 커밋하면 SSOT 손상. 반드시 통과한 후 커밋.
- **순차 실행**: 기계 검증 먼저 실행하고 오류 해결, 그 후 컨텐츠 검증.
- **기한**: 각 ingest 당일 또는 다음 날까지 검증 완료.
- **로그 기록**: 검증 완료 후 `log.md`에 `[REVIEW]` 항목 추가.

### 사용 예시

```bash
# 기본 검증 실행
/validate-wiki 2026-06-11

# 상세 모드 (토픽별 분석 포함)
/validate-wiki 2026-06-11 --verbose

# 리포트 파일로 저장
/validate-wiki 2026-06-11 --save-report

# 특정 토픽만 검증
/validate-review 2026-06-11 --focus=결제-연동,정산-주기

# 엄격 모드 (작은 불일치도 플래그)
/validate-wiki 2026-06-11 --strict
```

## 규칙 (DO/DON'T)

### ✅ 꼭 해야 할 규칙

**구조**
- raw.md는 저장 후 절대 수정 금지 (write-once). 오타도 건드리지 말 것.
- 신규 토픽 페이지는 반드시 폴더 노트 형식: `wiki/topics/슬러그/슬러그.md`
- 신규 토픽 생성 후 즉시 `index.md`에 등록

**링크**
- raw 참조: 항상 `../../../raw/YYYY-MM-DD_슬러그/meta.md` (3단계, 일관성 유지)
- 형제 토픽: 항상 `../슬러그/슬러그.md` (상대경로, 일관성 유지)
- ingest 완료 전 모든 신규 링크 검증 (깨진 링크 허용 안 함)

**업데이트**
- ingest 후 반드시 `wiki/결정/결정.md` 갱신
- ingest 후 반드시 `wiki/액션아이템/액션아이템.md` 갱신
- 기존 내용과 모순되면, 지우지 말고 변경 이력 남기기: "2026-08-10 변경: 이전 X → 현재 Y ([링크])"
- `log.md`에 `[INGEST]` 항목 추가

**투명성**
- 주제 페이지에 근거 링크 필수 (어느 회의에서 나온 정보인지)
- meta.md는 회의 원문의 가벼운 카드 역할 (요약, 참석자, 일시 등)

### ❌ 절대 하지 말 규칙

**구조**
- `wiki/topics/슬러그.md` 형식 사용 금지 (평면 구조, 구 형식)
- 같은 주제에 여러 슬러그 페이지 생성 금지 (중복 주제 페이지)

**링크**
- raw 링크를 `../../raw/`, `../../raw/`, `../../../../raw/` 등으로 섞어 쓰기 금지
- Obsidian `[[위키링크]]` 사용 금지 (마크다운 표준 링크만 사용)

**raw.md**
- raw.md 저장 후 수정 금지 (오타 포함)
- 정정이 필요하면 주제 페이지에 각주로 "역정정" 남기기

**업데이트**
- ingest 후 결정.md, 액션아이템.md 갱신 누락 (SSOT 깨짐)
- 신규 토픽 생성했는데 index.md에 등록 안 함 (고아 페이지 생성)

**동시성**
- 같은 토픽 페이지를 인간과 AI가 동시 편집 금지 (merge 충돌 위험)
- ingest 진행 중 그 주제 페이지 사용자 편집 금지

### 💡 선택적이지만 권장 (NICE-TO-HAVE)

- frontmatter에 `updated`, `topics`, `status` 필드 (최소 updated 필드)
- 주제 페이지마다 "관련 토픽" 섹션 (다른 토픽으로의 양방향 역링크)
- raw.md에 최소 섹션 구조 (### 논의 내용, ### 결정사항, ### 액션아이템)

## 스타일 가이드

- **링크**: 표준 상대경로 마크다운 링크 사용.
  `[텍스트](../../raw/2026-08-10_회의명/meta.md)`
  **Obsidian `[[위키링크]]` 문법은 절대 사용 금지** (일반 마크다운 도구/grep과의 호환성 우선).
  추후 Obsidian 연동을 원하면 이 규칙을 조정 가능.

- **frontmatter**: 주제 페이지에 선택적 frontmatter 권장 (강제 아님).
  ```yaml
  ---
  updated: 2026-08-10
  topics: [주제1, 주제2]
  status: active
  ---
  ```
  최소한 `updated` 필드는 마지막 갱신 시마다 갱신.

- **회의록 특유 정보**: 참석자, 결정, 액션아이템은 자유 형식 산문/불릿으로 서술.
  엄격한 필드 강제 없음. 자연스러운 문장으로 본문에 녹임.

- **raw.md 포맷**: 완전 자유형식 허용. 하지만 다음 섹션 구조 권장:
  - `### 논의 내용` / `### 핵심 포인트`
  - `### 결정사항`
  - `### 액션아이템` / `### 할 일`
  
  이렇게 하면 meta.md에서 쉽게 요약 가능.

## 핵심 철학

AI 협업 도구는 반복적인 작업(누적, 상호링크, 수정 이력 추적)을 인간보다 효율적으로 처리할 수 있으므로,
이 시스템이 지속가능하다. 자동화 가능한 빈도로 위키를 갱신·검증하고,
회의가 쌓일수록 지식의 진가가 드러난다.

**사용 예시**:
- Claude Code (Claude의 공식 개발 도구)
- GitHub Copilot (GitHub의 AI 어시스턴트)
- Codex (OpenAI의 코드 생성 모델)
- 기타 마크다운 기반 프로젝트 관리 도구

모든 도구는 동일한 스키마와 규칙을 따릅니다.
